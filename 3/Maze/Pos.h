#ifndef POS_H
#define POS_H

// Position structure to represent a point in a 2D space
struct Pos {
    int x, y;
    // Constructor to initialize x and y coordinates
    Pos(int x, int y);
};

#endif // POS_H
